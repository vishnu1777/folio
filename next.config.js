/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['pin.it', 'images.unsplash.com'] // Add other domains as needed like 'images.unsplash.com', 'imgur.com', etc.

    },
};

module.exports = nextConfig;